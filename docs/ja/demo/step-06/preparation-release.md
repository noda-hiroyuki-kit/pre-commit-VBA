---
icon: lucide/sheet
---
# Excelブックのリリース準備

バージョン `v0.1.0`をリリースすることとして, 記述する.

## `release/v[セマンティック バージョニング]`ブランチの作成

1. `develop`ブランチより, `release/v0.1.0`ブランチを作成し、切り替える.
    ```powershell
    git switch -c release/v0.1.0
    ```
